node_modules/
dist/
# Coverage
coverage/
# Playwright e2e artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/
# Generated gRPC-web/protobuf stubs (produced by `yarn gen`, like the Flutter app's).
src/gen/
# Generated wasm modules (produced by `yarn gen:wasm`). The SoundFont is no longer
# staged here — it is served by the backend delivery route at runtime.
src/wasm/pkg/
src/wasm/pkg-audio/
*.local
.env

# Yarn Berry (node-modules linker): commit yarn.lock + .yarnrc.yml, ignore the rest.
.yarn/*
!.yarn/patches
.pnp.*

# Vite dep-optimization cache
.vite/
