# Yarn Berry with the classic node_modules layout (not PnP), like apps/back-office.
nodeLinker: node-modules
# The site was born outside the monorepo on a machine with a private npm registry
# (ADEO) — pin the public registry so `yarn install` resolves the same everywhere.
npmRegistryServer: "https://registry.npmjs.org"
