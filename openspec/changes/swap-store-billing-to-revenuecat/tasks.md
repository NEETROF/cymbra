## 1. Spec ordering + RevenueCat project (D8 step 2 — no code)

- [x] 1.1 Archived `add-premium-subscription` (2026-08-18-add-premium-subscription, +34/~6 requirements merged into `openspec/specs/`, incl. `music-subscription-billing` + `music-premium-paywall`); D7/D8 of its `design.md` carry the "superseded by swap-store-billing-to-revenuecat (2026-08-18)" note
- [x] 1.2 Create the RevenueCat project + apps (App Store: bundle `com.cymbra.music` iOS + macOS; Play: package `com.cymbra.music`); entitlement `premium` attached to `premium_monthly` / `premium_yearly` on each store; restore behaviour **Keep with original App User ID**; record project id, public SDK keys (Apple / Google) and the v1 secret API key — *done 2026-08-20: project `proj4adaf652`, apps `Cymbra Music (App Store)` (`app1f6b1e942d`, IAP key valid) + `Cymbra Music (Play)` (`app193dcee49a`), entitlement `premium`, restore = keep-with-original; wizard leftovers (Cymbra Pro, monthly/yearly/lifetime, default offering packages) cleaned; public keys on each app page, v1 secret key still to issue*
- [x] 1.3 App Store side: In-App Purchase key (.p8) uploaded to RevenueCat, App Store Server Notifications v2 URL (production + sandbox) pointed at RevenueCat's URL, subscription group + products per the existing checklist; Play side: service-account JSON with Play Developer API + financial data access uploaded to RevenueCat, RTDN Pub/Sub topic pointed at RevenueCat's topic; note in `apps/music/store/SUBSCRIPTIONS.md` that these secrets no longer live in our environment — *Apple half done 2026-08-20: .p8 valid in RC, ASN v2 (prod+sandbox) → RC URL, `premium_monthly`/`premium_yearly` Ready to Submit in ASC, created in RC and attached to `premium`. Google 2026-08-21: merchant account + 15% program, SA `revenuecat@cymbra.iam` (Pub/Sub Editor + Monitoring Viewer, 3 APIs enabled, org-policy `iam.disableServiceAccountKeyCreation` overridden on project `cymbra` only), JSON uploaded, Play subs `premium_monthly`/base `monthly` + `premium_yearly`/base `yearly` ACTIVE (7,99 €/49,99 € TTC FR — Play's Set prices is tax-EXCLUSIVE: bulk 6,66/41,66 HT), RC products `premium_monthly:monthly` + `premium_yearly:yearly` attached → `premium` = 4 products. Done 2026-08-22: credential checks "Valid credentials" (root cause: Google Play Android Developer API was never actually enabled — enabling it fixed the checks instantly, no propagation wait); RTDN Connected (topic `projects/cymbra/topics/Play-Store-Notifications`, sub `RevenueCat-Subscriber-app193dcee49a`), Play test notification OK after granting `google-play-developer-notifications@system.gserviceaccount.com` the Pub/Sub Publisher role on the topic (org policy `iam.allowedPolicyMemberDomains` overridden to Allow-all on project `cymbra` first — RC's Editor SA can't setIamPolicy), RC shows "Last received 2026-08-22 08:30 UTC"*
- [x] 1.4 RevenueCat webhook: URL `https://api.cymbra.app/billing/revenuecat/webhook` (staging host too), generated `Authorization` header value → `CYMBRA_REVENUECAT_WEBHOOK_SECRET`; sign the RevenueCat DPA and record the transfer mechanism (SCCs) in `backend/plans/README.md` — *webhook "Cymbra backend" Active + secret in prod env (2026-08-21; no staging env exists). DPA 2026-08-22: nothing to sign — RC's DPA (revenuecat.com/dpa, effective Aug 2026) is incorporated by reference into the ToS accepted at signup (no dashboard signature flow); EU SCCs module 2 + Annex 3 sub-processor list recorded in `backend/plans/README.md`*

## 2. Backend: RevenueCat adapter (D1, D3, D4)

- [x] 2.1 `backend/plans/src/billing/revenuecat.rs`: `RcConfig { webhook_secret, api_key, project_id, allow_sandbox }`; `store_to_source(store) -> Option<Source>` (`APP_STORE|MAC_APP_STORE→apple`, `PLAY_STORE→google`, `PADDLE→web`, else None); typed `RcEvent` (serde, tolerant of unknown fields/types) with `id, type, app_user_id, store, environment, product_id, new_product_id, original_transaction_id, expiration_at_ms, grace_period_expiration_at_ms, cancel_reason, expiration_reason, transferred_from/to`
- [x] 2.2 Pure mapper `map_event(&RcEvent, premium_products, now, allow_sandbox) -> Vec<EntitlementWrite> | Skip(reason)` implementing the D4 table (forward-only ends_at, grace, cancelled-keeps-end, refund-ends-now, expiration, pause, product change, transfer, no-op types, unmapped store/product, malformed uuid, sandbox rule); table-driven tests from RevenueCat sample payloads checked in as JSON fixtures (`billing/fixtures/revenuecat/*.json`)
- [x] 2.3 Pure mapper `map_customer(&RcSubscriber, premium_products, now, allow_sandbox) -> Vec<EntitlementWrite>` for the REST subscriber payload (`subscriptions{product_id → expires_date, purchase_date, original_purchase_date, store, is_sandbox, unsubscribe_detected_at, billing_issues_detected_at, grace_period_expires_date, refunded_at, store_transaction_id/original id}`); same rules as 2.2; test that a webhook `RENEWAL` and the corresponding customer fetch produce the same `(source, provider_ref, ends_at, status)` — this is the D1 key-consistency check (assert on the fixture pair)
- [x] 2.4 `RcClient` (reqwest, thin — on the coverage ignore list): `get_subscriber(app_user_id)`, `delete_subscriber(app_user_id)`; `#[automock]` port traits `StoreCustomerSource` (get) and `StoreCustomerEraser` (delete) in `ports.rs`; per the `rust-testing` convention
- [x] 2.5 Webhook handler `handle_webhook(svc, cfg, headers, body, now)`: constant-time `Authorization` compare **before** body parse (401, no side effect, counter), parse, per-store `billing.<channel>.enabled` via `PaywallConfigSource::channel_enabled` (disabled ⇒ 200 + logged skip), `ingest(ProviderEvent { provider: revenuecat, event_id: event.id, writes with source = store })` (the event provider is `revenuecat`; the ledger rows keep the store as `source`), duplicates ⇒ 200; tests: replay, bad auth, disabled channel, unmapped store, refund, transfer
- [x] 2.6 Migration `backend/plans/migrations/0002_revenuecat.sql`: widen `billing_events.provider` CHECK to include `'revenuecat'`; `BillingEventRepo::record_if_new` accepts the new provider (adjust the `Source`/provider type split — provider is not a ledger `Source`); integration test
- [x] 2.7 `PlanService::sync_store_plan(user_id, now)`: fetch customer via `StoreCustomerSource`, `map_customer`, upsert forward-only, return the plan view; used by 3.1 and 3.2; tests with the mocked source: active sub, refunded, none, sandbox-in-prod ignored, third-party error → typed error (never a grant)

## 3. Backend: surface, reconcile, erasure, removal (D3, D6, D8)

- [x] 3.1 `backend/plans/proto/plans.proto`: remove `ReportStorePurchase` (+ messages) and the restore doc line; add `rpc SyncStorePlan(SyncStorePlanRequest{}) returns (SyncStorePlanResponse{ GetMyPlanResponse plan })`; `grpc.rs`: implement behind `ratelimit::check` per user; `plans.enabled` off ⇒ returns the free view without calling the aggregator; tests
- [x] 3.2 `backend/plans/src/billing/reconcile.rs`: replace the Apple/Google/web reconcilers with one `Reconciler` over `StoreCustomerSource` (`rows ending within N days without an event in the last M days` → `sync_store_plan` per user, dedup by user); keep the `plans_reconcile` job wiring in `backend/worker/src/handlers.rs` (`Reconcilers` → the single reconciler; `Default` = disabled); tests: missed renewal repaired, refund caught, aggregator error leaves the row untouched
- [x] 3.3 Erasure: add `rc_erase: Option<Arc<dyn StoreCustomerEraser>>` to `WorkerCtx`, call `delete_subscriber(user_id)` in `purge_user` **before** `PlanService::purge_user`, keep the Paddle `web_cancel`; failure ⇒ job error (retried), never swallowed; regression test with the mock eraser
- [x] 3.4 `billing/env.rs`: `RevenueCatEnv { api_key, webhook_secret, project_id, allow_sandbox }` from `CYMBRA_REVENUECAT_API_KEY`, `CYMBRA_REVENUECAT_WEBHOOK_SECRET`, `CYMBRA_REVENUECAT_PROJECT_ID`, `CYMBRA_REVENUECAT_ALLOW_SANDBOX`; drop `AppleEnv` / `GoogleEnv`; `BillingChannels::build` yields `rc_client`, `rc_eraser`, `web` (Paddle unchanged); update `backend/server/src/main.rs` (`PlanGrpc` no longer takes store verifiers) and `backend/worker/src/main.rs`
- [x] 3.5 `backend/server/src/billing.rs`: mount `/billing/revenuecat/webhook` when the secret is set; remove `/billing/apple/notifications` and `/billing/google/rtdn`; keep `/billing/web/webhook`; route tests (401 without header, 200 on TEST event)
- [x] 3.6 Delete `backend/plans/src/billing/apple.rs`, `google.rs`, the bundled Apple root CAs / fixtures, `StorePurchaseVerifier` port + `Channel`-keyed verifiers in `grpc.rs`/`service.rs`, the `x509-parser`, `p256`, `p384`, `rcgen` deps (keep `jsonwebtoken`/`hmac` only if Paddle still needs them); `cargo udeps`-style check that nothing else pulls them; update the crate description and `README.md`
- [x] 3.7 Coverage: replace the Apple/Google paths in the `cargo llvm-cov --ignore-filename-regex` list (CI workflow + CLAUDE.md snippet) with `billing/revenuecat.rs`'s HTTP glue only (mappers stay covered); `.env.example`: drop `CYMBRA_APPLE_ASC_*`, `CYMBRA_APPLE_ALLOW_SANDBOX`, `CYMBRA_GOOGLE_SA_*`, `CYMBRA_GOOGLE_RTDN_*`, `CYMBRA_GOOGLE_PACKAGE_NAME`; add the `CYMBRA_REVENUECAT_*` block with comments

## 4. App: `RevenueCatStoreClient` + `SyncStorePlan` (D2)

- [x] 4.1 `apps/music/pubspec.yaml`: add `purchases_flutter`, remove `in_app_purchase` (+ platform sub-packages if listed); iOS/macOS/Android platform setup per the SDK (StoreKit 2 default, macOS App Store build capability); `LANG=en_US.UTF-8 pod install`
- [x] 4.2 `lib/services/store_client.dart`: `RevenueCatStoreClient implements StoreClient` — configure with the platform public key from `String.fromEnvironment('CYMBRA_RC_APPLE_KEY'|'CYMBRA_RC_GOOGLE_KEY')` (empty ⇒ provider yields `NoopStoreClient`), `products(ids)` via `getProducts`, `buy` via the SDK purchase API mapping `PurchasesErrorCode` → `StoreEvent.cancelled | pending | otherAccount (new variant, from receiptAlreadyInUse) | error`, success → `StoreEvent.receipt(StoreReceipt(productId, payload: ''))`, `restore` → one `receipt(restored: true)` if an active entitlement/subscription is present else a new `StoreEvent.nothingToRestore`, `complete` no-op; **no attribute API exposed**; keep the plugin behind the seam so widgets/tests never import `purchases_flutter`
- [x] 4.3 Identity: `StoreClient.setAccount(String? userId)` (`logIn` / `logOut`) called from the existing session listener (`session_notifier.dart` / the plan listener widget) on sign-in and sign-out; `NoopStoreClient` ignores it; test that sign-out resets before the next sign-in configures
- [x] 4.4 `lib/state/plan_notifier.dart` `PurchaseFlow`: on `receipt` → `planService.syncStorePlan()` (new client method) then `adopt(view.plan)`; `otherAccount` / `nothingToRestore` from store events (drop the 3 s restore delay and the `permissionDenied` branch); web checkout branch untouched; localized `pending` outcome when sync fails after a store success (plan refresh on resume retries)
- [x] 4.5 Regenerate stubs (`melos gen-grpc`) and codegen (`build_runner`); ARB strings en/fr/es/it for the new/changed outcomes only (`another account`, `nothing to restore`, `sync pending`)
- [x] 4.6 Tests (mockito per `flutter-testing`): mocked `StoreClient` + mocked plan service — purchase → sync → premium; restore found / nothing / other account; sync failure keeps last-known plan and shows pending; sign-out resets identity; no `purchases_flutter` import outside `store_client.dart` (analyzer/grep guard in a test)

## 5. Back office + docs + legal (D5, D6)

- [x] 5.1 `apps/back-office`: `plans` store/view — "open in RevenueCat" link on the account lookup (`https://app.revenuecat.com/customers/<project>/<user_id>`, project id from `VITE_REVENUECAT_PROJECT_ID`, hidden when unset); regenerate the client (`yarn gen`) for the proto change; component test
- [x] 5.2 `apps/music/store/SUBSCRIPTIONS.md`: rewrite the Apple / Google sections for the RevenueCat setup (keys and notification URLs configured in RevenueCat, restore behaviour, webhook, sandbox testers still needed), keep Paddle direct, add the "Rollout order" step for `CYMBRA_REVENUECAT_ALLOW_SANDBOX`; `backend/plans/README.md`: purchase channels + runbook (rotate the webhook secret / API key, disable a store, refund path via the store console + RevenueCat customer page, erasure, where revenue is read)
- [x] 5.3 Legal: name RevenueCat as sub-processor (purpose: subscription verification and analytics; data: opaque account id + store transaction facts; US, SCCs) in `docs/legal/privacy-policy.md` + `politique-de-confidentialite.md`, and align the site pages `apps/site/src/pages/confidentialite.md` + `en/privacy.md` (which today do not mention the purchase channels at all); terms unchanged
- [x] 5.4 `add-premium-subscription` tasks 11.5–11.8 moved to §7 (7.10–7.14) and 11.9/11.10 marked superseded before its archive (done 2026-08-18)

## 6. Verification

- [x] 6.1 `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo llvm-cov --workspace --fail-under-lines 80` green (mappers + service covered; only the reqwest glue ignored)
- [x] 6.2 `melos run analyze`, `dart run custom_lint`, `dart format`, `flutter test --coverage` green; `flutter build` for iOS, macOS, Android with the RC keys, and Linux/Windows without (Noop path)
- [x] 6.3 Back office `yarn lint`, `yarn test`, e2e green (own `BO_E2E_PORT`)
- [x] 6.4 `openspec validate swap-store-billing-to-revenuecat --strict` passes

## 7. Manual — sandbox through RevenueCat (D8 step 3)

- [x] 7.1 Dark: webhook secret unset ⇒ route absent; set ⇒ RevenueCat "send test event" returns 200 and a `TEST` no-op is counted; wrong header ⇒ 401 — *2026-08-22 on prod (backend 0.19.0 deployed): POST without auth ⇒ 401 (route mounted, Caddy passes the prefix); RC "Send Test Event" ⇒ Response 200 in the dashboard (TEST payload, environment SANDBOX)*
- [ ] 7.2 iOS sandbox tester: purchase `premium_monthly` → RevenueCat customer shows the subscription → webhook `INITIAL_PURCHASE` lands (`billing_events` row, `apple` entitlement row) → app `SyncStorePlan` shows premium; accelerated renewal moves `ends_at`; cancel keeps access to the end; sandbox refund ends now; verify the D1 key-consistency (webhook `original_transaction_id` == subscriber fetch key) — if not equal, apply the documented fallback key and re-test
- [ ] 7.3 macOS App Store build: same purchase appears as `MAC_APP_STORE` → `apple`; iPhone signed in as the same account shows premium without a second purchase
- [ ] 7.4 Android license tester: purchase → `PLAY_STORE` → `google` row; RTDN-driven renewal / cancel / on-hold (`BILLING_ISSUE` grace) / refund; pause → row ended, resume → renewal
- [ ] 7.5 Restore: second device same account → restored; second account → SDK `receiptAlreadyInUse` → "another account", first account's row unchanged; reinstall + restore → premium
- [ ] 7.6 Reconciliation: stop the webhook (wrong secret) → let a sandbox renewal happen → `plans_reconcile` repairs the row; refund without event → caught
- [ ] 7.7 Erasure: delete a sandbox account → RevenueCat customer gone, plan rows purged
- [ ] 7.8 Visibility: RevenueCat Overview / Charts show the sandbox actives, MRR and current-month revenue per store; BO lookup link opens the customer page
- [ ] 7.9 Production flags: `billing.apple.enabled` on → real purchase on TestFlight (production environment) lands; then `billing.google.enabled`

Plan validations inherited from `add-premium-subscription` 11.5–11.9 (moved here 2026-08-18; independent of the aggregator, run in the same staging session):

- [ ] 7.10 Manual — dark deploy: all flags off ⇒ app identical to before, the RevenueCat webhook acknowledges and ignores, `GetMyPlan` says free
- [ ] 7.11 Manual — withdrawal: let a trial expire on an account with cached catalog scores, own uploads, an imported .sf2 and a premium .sf2 → reconnect: catalog cache and premium font gone, upload cache + import intact, notice shown; then re-subscribe in sandbox (through RevenueCat) → content re-fetchable, favorites intact
- [ ] 7.12 Manual — trial: create a 90-day trial campaign, mint a code, redeem on the web, verify premium on iOS/Android/macOS/Linux/Windows builds of the same account, close enrolment and confirm the trial keeps running, revoke a membership and confirm immediate degradation
- [ ] 7.13 Manual — feature beta: create `midi-drums`, scope a flag `beta:midi-drums`, enrol one free and one premium account, confirm only members see the feature, close the campaign, confirm the feature disappears for members without a flag edit
- [ ] 7.14 Manual — precedence: trial tester purchases in the App Store sandbox (through RevenueCat) ⇒ still premium after the trial ends; sandbox refund ⇒ back to trial (if running) or free

## 8. Deferred — Paddle into RevenueCat (D7; only when Windows / Linux purchases open)

- [ ] 8.1 Manual purchase tracking: on the HMAC-verified `transaction.completed`, forward `fetch_token = <paddle subscription id>` + `app_user_id = <cymbra user id from custom_data>` to RevenueCat `POST /v1/receipts` (idempotent on the Paddle transaction id); Paddle API key connected in the RevenueCat project
- [ ] 8.2 Once `PADDLE` events flow on the RevenueCat webhook (`source = web`), retire the direct Paddle entitlement mapper in `billing/web.rs` (keep checkout, portal, cancel); disabled-channel and idempotency tests
- [ ] 8.3 Sandbox from a Windows / Linux build: checkout → RevenueCat customer + webhook → premium; iOS/Android show "managed on the web"; RevenueCat Charts show the web store
