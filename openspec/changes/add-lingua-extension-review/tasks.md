# Tasks — add-lingua-extension-review

## 1. Review inside the extension (spec lingua-browser-extension)

- [x] 1.1 Side panel (deck, FSRS review session, answer hidden/revealed, due counter) + a collapsed injected drawer sharing the same logic; `sidePanel` permission added to the manifest
- [x] 1.2 Versioned storage with migrations + reset; backup (download) and restore (re-import) from the side panel
- [x] 1.3 Attributions page (the pack's NOTICE) + a privacy note stating "nothing leaves the device"
- [x] 1.4 Manual verification: load unpacked, full walkthrough (calibration → reading → +Deck → review → export) on 5 real sites
