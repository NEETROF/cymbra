include: package:flutter_lints/flutter.yaml

analyzer:
  plugins:
    - custom_lint
  exclude:
    # Generated code (proto stubs + riverpod codegen) is not linted.
    - lib/src/grpc/**
    - "**/*.g.dart"
