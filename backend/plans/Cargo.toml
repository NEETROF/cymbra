[package]
name = "cymbra-plans"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Cymbra plans — the free/premium entitlement ledger (multi-source, expiring), beta campaigns + memberships (premium trials, feature betas), single-use access codes, and the purchase-channel adapters (store aggregator for App Store / Play, web merchant-of-record). Owns the `plans` schema. Stores identifiers only — never billing personal data. Changes: add-premium-subscription, swap-store-billing-to-revenuecat."

[dependencies]
async-trait.workspace = true
tracing.workspace = true
sqlx.workspace = true
serde = { workspace = true }
serde_json.workspace = true
uuid.workspace = true
anyhow.workspace = true
thiserror.workspace = true
tokio.workspace = true
chrono = { workspace = true, features = ["serde"] }
# Access codes: 128-bit CSPRNG, base32-ish clear text, SHA-256 at rest.
getrandom = "0.2"
sha2 = "0.10"
# Provider webhooks / APIs (store aggregator customer API, web MoR): reqwest;
# HMAC for the web MoR webhook signature.
reqwest.workspace = true
hmac.workspace = true
# gRPC PlanService (client + admin surface).
tonic.workspace = true
prost.workspace = true
cymbra-platform = { path = "../platform" }
# `MockEntitlementRepo` & co. via `#[automock]` under the `mock` feature so the
# server / music crates can double the plan seams (rust-testing default).
mockall = { workspace = true, optional = true }

[build-dependencies]
tonic-build.workspace = true

[features]
mock = ["dep:mockall"]

[dev-dependencies]
tokio = { workspace = true }
mockall = { workspace = true }
