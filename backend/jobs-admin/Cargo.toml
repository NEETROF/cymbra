[package]
name = "cymbra-jobs-admin"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Cymbra Jobs console — the back-office JobsAdminService over the job queue: listing, per-state and per-period figures, and safe cancellation (change: add-admin-jobs-console)."

[dependencies]
# The substrate it administers: the registry (protected kinds) and the history retention.
cymbra-jobs = { path = "../jobs" }
# The admin session identity, the scope guard and AppError ⇄ Status.
cymbra-platform = { path = "../platform" }
async-trait.workspace = true
anyhow.workspace = true
chrono.workspace = true
sqlx.workspace = true
tonic.workspace = true
prost.workspace = true
tracing.workspace = true
uuid.workspace = true

[build-dependencies]
tonic-build.workspace = true

[dev-dependencies]
tokio = { workspace = true }
mockall = { workspace = true }
