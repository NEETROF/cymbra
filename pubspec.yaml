# Root of the Dart/Flutter side of the monorepo — managed by Melos.
# This package is not published; it only hosts the Melos tooling.
name: cymbra_workspace
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  melos: ^6.3.0
